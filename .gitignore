# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local
.env
functions/.env

# Secrets — never commit
serviceAccountKey.json
serviceAccountKey*.json

# Local tool/deploy caches
.firebase/
.remember/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.obsidian/
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Browser-automation artifacts
.playwright-mcp/

# Local review/analysis artifacts. These are working notes from review passes
# (Codex, agent runs, knowledge-graph exports), not part of the product, and
# they must never land in a product commit.
.agents/
.codex/
graphify-out/
